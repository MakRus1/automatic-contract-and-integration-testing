# Автоматизация контрактного и интеграционного тестирования

![CI Status](https://github.com/MakRus1/automatic-contract-and-integration-testing/actions/workflows/ci.yml/badge.svg)

Демонстрационный проект на C++ для изучения автоматизации контрактного и интеграционного тестирования с использованием GitHub Actions.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Типы тестов](#типы-тестов)
- [Сборка и запуск](#сборка-и-запуск)
- [GitHub Actions CI/CD](#github-actions-cicd)
- [Структура проекта](#структура-проекта)

## 📖 Описание проекта

Проект демонстрирует подходы к тестированию программного обеспечения:

1. **Контрактное тестирование** — проверка соответствия реализации заданному интерфейсу (контракту)
2. **Интеграционное тестирование** — проверка корректного взаимодействия компонентов системы
3. **Unit тестирование** — проверка отдельных компонентов в изоляции

### Предметная область

Простая система управления пользователями и заказами:
- **UserService** — управление пользователями (создание, деактивация)
- **OrderService** — управление заказами (создание, отмена, изменение статуса)
- **InMemoryDatabase** — хранение данных в памяти

## 🏗 Архитектура

```
┌─────────────────┐     ┌─────────────────┐
│   UserService   │     │   OrderService  │
│  (IUserService) │◄────│ (IOrderService) │
└────────┬────────┘     └────────┬────────┘
         │                       │
         │    ┌──────────────┐   │
         └────►  IDatabase   ◄───┘
              │(InMemoryDB)  │
              └──────────────┘
```

### Контракты (интерфейсы)

- `IUserService` — контракт сервиса пользователей
- `IOrderService` — контракт сервиса заказов  
- `IDatabase` — контракт базы данных

## 🧪 Типы тестов

### Unit тесты (`tests/unit/`)

Тестируют отдельные компоненты в изоляции:

```cpp
TEST_F(UserServiceUnitTest, CreateUser_ValidData_ReturnsPositiveId) {
    int id = userService_->createUser("John Doe", "john@example.com");
    EXPECT_GT(id, 0);
}
```

### Контрактные тесты (`tests/contract/`)

Проверяют соответствие реализации контракту (предусловия, постусловия, инварианты):

```cpp
TEST_F(UserContractTest, CreateUser_Contract_EmptyName_ReturnsMinusOne) {
    // Arrange: нарушено предусловие (пустое имя)
    std::string empty_name = "";
    
    // Act
    int result = service_->createUser(empty_name, "john@test.com");
    
    // Assert: постусловие - возврат -1
    EXPECT_EQ(result, -1) 
        << "CONTRACT VIOLATION: createUser must return -1 for empty name";
}
```

### Интеграционные тесты (`tests/integration/`)

Проверяют сквозные сценарии работы системы:

```cpp
TEST_F(UserOrderIntegrationTest, FullUserOrderLifecycle) {
    // 1. Регистрация пользователя
    int user_id = userService_->createUser("Иван", "ivan@test.com");
    
    // 2. Создание заказа
    int order_id = orderService_->createOrder(user_id, "Ноутбук", 75000.0);
    
    // 3. Подтверждение и отправка
    orderService_->updateOrderStatus(order_id, OrderStatus::CONFIRMED);
    orderService_->updateOrderStatus(order_id, OrderStatus::SHIPPED);
    
    // 4. Проверка состояния
    auto order = orderService_->getOrder(order_id);
    EXPECT_EQ(order->status, OrderStatus::SHIPPED);
}
```

## 🔧 Сборка и запуск

### Требования

- CMake 3.14+
- C++17 компилятор (GCC 7+, Clang 5+, MSVC 2017+)
- Git (для загрузки Google Test)

### Сборка

```bash
# Создание директории сборки
mkdir build && cd build

# Конфигурация
cmake ..

# Сборка
cmake --build .
```

### Запуск тестов

```bash
# Все тесты через CTest
ctest --output-on-failure

# Или отдельно:
./unit_tests          # Unit тесты
./contract_tests      # Контрактные тесты
./integration_tests   # Интеграционные тесты
```

### Запуск с подробным выводом

```bash
./unit_tests --gtest_filter="*CreateUser*"  # Только тесты CreateUser
./contract_tests --gtest_list_tests         # Список всех тестов
./integration_tests --gtest_repeat=10       # Повторить 10 раз
```

## 🚀 GitHub Actions CI/CD

Проект автоматически тестируется при каждом push и pull request.

### Workflow включает:

1. **Сборка и тестирование** на Ubuntu и macOS (Debug и Release)
2. **Статический анализ** с помощью cppcheck
3. **Измерение покрытия кода** с помощью lcov

### Матрица тестирования:

| ОС | Debug | Release |
|:---|:---:|:---:|
| Ubuntu | ✅ | ✅ |
| macOS | ✅ | ✅ |

### Артефакты:

- Результаты тестов в формате XML
- Отчет о покрытии кода

## 📁 Структура проекта

```
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions workflow
├── include/
│   ├── contracts/              # Интерфейсы (контракты)
│   │   ├── user_contract.hpp
│   │   ├── order_contract.hpp
│   │   └── database_contract.hpp
│   └── services/               # Заголовки реализаций
│       ├── user_service.hpp
│       ├── order_service.hpp
│       └── database.hpp
├── src/                        # Реализации
│   ├── user_service.cpp
│   ├── order_service.cpp
│   └── database.cpp
├── tests/
│   ├── unit/                   # Unit тесты
│   │   ├── user_service_test.cpp
│   │   └── order_service_test.cpp
│   ├── contract/               # Контрактные тесты
│   │   ├── user_contract_test.cpp
│   │   └── order_contract_test.cpp
│   └── integration/            # Интеграционные тесты
│       └── user_order_integration_test.cpp
├── CMakeLists.txt
└── README.md
```
